# CLAUDE.md

This file guides Claude Code (and other AI assistants) when working in this repository.

> **Status: placeholder.** This repository currently has no commits or source
> files. The sections below are a starting template — replace each one with
> real details as the codebase is built out, so future AI assistants have
> accurate context instead of guesses.

## Project Overview

- **What is this project?** _TODO: one-paragraph description of the product/service and its purpose._
- **Primary language(s) / framework(s):** _TODO_
- **Repository:** `bernardoabreu-sup/superside-test`

## Getting Started

_TODO once tooling exists. Typically covers:_

```bash
# install dependencies
# e.g. npm install / pip install -r requirements.txt / bundle install

# run the app locally
# e.g. npm run dev

# run tests
# e.g. npm test
```

## Repository Structure

_TODO: fill in once directories exist, e.g._

```
.
├── src/          # application source
├── tests/        # test suite
└── ...
```

## Development Workflow

- **Branching:** _TODO — describe branch naming, e.g. `feature/*`, `fix/*`._
- **Commits:** _TODO — describe commit message conventions (e.g. Conventional Commits)._
- **Pull requests:** _TODO — describe PR requirements (reviews, CI checks, templates)._
- **CI/CD:** _TODO — describe pipelines once configured (GitHub Actions, etc.)._

## Coding Conventions

- **Style/formatting:** _TODO — linter/formatter and config (ESLint, Prettier, Black, gofmt, etc.)._
- **Testing conventions:** _TODO — test framework, file naming, coverage expectations._
- **Type checking:** _TODO — if applicable (TypeScript, mypy, etc.)._

## Key Commands

_TODO: list the actual scripts/commands once defined, e.g._

| Command | Purpose |
|---|---|
| `npm run build` | Build the project |
| `npm test` | Run the test suite |
| `npm run lint` | Lint the codebase |

## Notes for AI Assistants

- Keep this file up to date as the project structure, tooling, and conventions solidify.
- Prefer editing existing files over creating new ones; avoid introducing new abstractions unless the codebase already establishes a pattern for them.
- When conventions are unclear or undocumented, ask before assuming.
